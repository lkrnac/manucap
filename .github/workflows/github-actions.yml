name: manucap CI/CD
run-name: ${{ github.actor }} manucap CI/CD
on: [push]
jobs:
    unit-tests-regression:
        runs-on: ubuntu-latest
        steps:
            - run: cd manucap
            - run: npm i
            - run: npm test
